# Formol dashboard app

## Technologies

- `NextJS` pour le routage, l'installation et l'ajout de composant rapide.
- `React` pour la librairie UI.
- `MaterialUI` pour la librairie de composant CSS.

## Architecture

*WIP*.

## Installation et lancement

Utiliser `yarn` ou `npm install` pour l'installation.

`yarn dev` ou `npm run dev` pour lancer le client de développement.

## Utilisation

*WIP*.
